<% if current_user %>
<table>
  <tr>
    <td>Logged in : </td>
    <td><strong><%= current_user.name %> (<%= current_user.nickname %>)</strong> | <%= link_to 'Settings', settings_path %> | <%= link_to 'Report Bug', 'http://track.reanstudios.net/projects/prb/issues/new' %></td>
  </tr>
  <tr>
    <td>Release :</td>
    <td><strong><%= File.split(RAILS_ROOT).last %></strong></td>
  </tr>
</table>
<% else %>
  <p><span class="sosumi">Not logged in |</span> <%= link_to 'Report Bug', 'http://track.reanstudios.net/projects/prb/issues/new' %></p>
<% end %>