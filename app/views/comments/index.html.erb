<h1>Listing comments</h1>

<table>
<% @comments.each do |comment| %>
  <tr>
    <td><%= CodeRay.scan(comment.body, :ruby).div(:line_numbers => :table)  %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New comment', new_comment_path %>